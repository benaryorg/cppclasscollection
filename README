Just in case I build something you want to use, just use it (see LICENSE).

You can also send me an email (binary@benary.org) so I know someone uses this and I could improve it.

Build like this:

~ $ mkdir build
~ $ cd build
~ $ cmake ..
~ $ make
~ # make install
